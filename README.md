# Naomi

Локальный чат-компаньон — живая тёплая собеседница. Просто болтает. Работает на **твоей подписке ChatGPT**, без платного API.

## Запуск

```sh
./run.sh        # → http://127.0.0.1:8765
```

Первый раз: ⚙️ → **Аккаунт** → «Войти через ChatGPT».

## Стек

- **`backend/`** — Python (FastAPI): запрос напрямую в подписку ChatGPT.
- **`frontend/`** — React без сборщика (esbuild).
- **`home/`** — модульный «умный дом»: датчики и живой контекст «[Сейчас]…».
- **`persona/`** — личность и правила (`naomi.md` / `pravila.md`), правятся на лету.
- **`docs/`** — документация.

Модель и уровень размышления — ⚙️ → **Агент** (по умолчанию `gpt-5.5` / `low`).

Подробности — [docs/DOCUMENTATION.md](docs/DOCUMENTATION.md).
